<h1><%= @task.title %></h1>

<p><strong>Details:</strong> <%= @task.details %></p>

<%= link_to "Edit task", edit_task_path(@task) %>
<%= link_to "Delete task", task_path(@task), data: { turbo_met: :delete, turbo_confirm: "Are you sure ?" } %>
<p><%= link_to "Back to tasks", tasks_path %></p>
